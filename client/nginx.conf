server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # React Router (BrowserRouter) SPA fallback: /interview must serve index.html.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
